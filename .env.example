# Copy this file to .env and fill in your real keys.
# Only the provider you set as activeProvider in master_prompt.dart is required.

OPENROUTER_API_KEY=
GLM_API_KEY=
VENICE_API_KEY=

# Required only for the voice coaching feature (realtime spoken sessions).
# Must be an OpenAI key with Realtime API access.
OPENAI_API_KEY=
